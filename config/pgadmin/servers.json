{
  "Servers": {
    "1": {
      "Name": "WissKI PostgreSQL",
      "Group": "Servers",
      "Host": "${DB_HOST}",
      "Port": "${DB_PORT}",
      "MaintenanceDB": "${DB_NAME}",
      "Username": "${DB_USER}",
      "SSLMode": "prefer",
      "PassFile": "/var/lib/pgadmin/.pgpass"
    }
  }
}
