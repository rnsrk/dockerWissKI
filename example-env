# General settings
MODE=production
USER_GROUPS=

# Adminer settings
ADMINER_PORT=8081

# Database settings
DB_DRIVER=mysql
DB_HOST=mariadb
DB_NAME=DATABASE
DB_PASSWORD=USERPW
DB_PORT=3306
DB_ROOT_PASSWORD=ROOTPW
DB_USER=DBUSER

# Drupal settings
DRUPAL_DOMAIN=localhost
DRUPAL_LOCALE=en
DRUPAL_PASSWORD=admin
DRUPAL_PORT=80
# When outside /opt/drupal/, ensure existence and permissions
DRUPAL_PRIVATE_FILES_DIR=/opt/drupal/private_files
DRUPAL_SITE_NAME=My WissKI
DRUPAL_START_PERIOD=300s
DRUPAL_TRUSTED_HOSTS='^localhost$|^127\.0\.0\.1$'
DRUPAL_USER=admin
# No Traefik: Drupal sees direct HTTP (port 80) or Varnish (port 8000) without TLS headers.
# Leave at none for local/standalone use. Set to auto when deploying behind Traefik (SCS).
DRUPAL_PROXY_ADDRESSES=none

# WissKI settings
WISSKI_BASE_IMAGE_VERSION=latest
# For a full list of WissKI recipe versions see: https://packagist.org/packages/soda-collection-objects-data-literacy/wisski_recipe_base
WISSKI_DEFAULT_DATA_MODEL_VERSION=1.x-dev
WISSKI_DEFAULT_GRAPH=https://my.wiss-ki.eu/
WISSKI_STARTER_VERSION=1.x-dev

# RDF4J settings
RDF4J_PORT=8080

# Redis settings
REDIS_HOST=redis
REDIS_PORT=6379

# SOLR settings
SOLR_PORT=8983

# TS settings
TS_USERNAME=ts_user
TS_PASSWORD=ts_password
TS_REPOSITORY=default # has to be "default" for now
TS_TOKEN=

# Varnish settings
VARNISH_PORT=8000
