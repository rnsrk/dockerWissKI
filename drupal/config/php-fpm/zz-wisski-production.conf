; WissKI production PHP-FPM pool. Sized for the 2G Drupal cgroup:
; memory_limit is 1G (ceiling for SPARQL), so 15 children would OOM.
[www]
pm = dynamic
pm.max_children = 6
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
pm.max_requests = 500
request_terminate_timeout = 300
