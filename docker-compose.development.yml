# Development: Drupal is the public HTTP entry. No Varnish.
# Loaded via COMPOSE_FILE in env/development.env.

services:
  drupal:
    ports:
      - ${HTTP_PORT:-80}:80
      - ${XDEBUG_PORT:-9003}:9003
    develop:
      watch:
        - action: rebuild
          path: ./drupal
