# Copy one of the presets to .env, then start:
#
#   cp env/development.env .env && docker compose up -d
#   cp env/production.env .env && docker compose up -d
#
# This file matches the development preset so a plain `cp .env.example .env` works.

COMPOSE_PROJECT_NAME=dockerwisski
COMPOSE_FILE=docker-compose.yml:docker-compose.override.yml:docker-compose.development.yml
COMPOSE_PROFILES=tools

MODE=development
WISSKI_IMAGE_VERSION=latest
WISSKI_PACKAGES_VERSION=3.6.0
WISSKI_PACKAGES_LINE=3.x

HTTP_PORT=80
XDEBUG_PORT=9003

DRUPAL_DOMAIN=localhost
DRUPAL_LOCALE=en
DRUPAL_USER=admin
DRUPAL_PASSWORD=admin
DRUPAL_SITE_NAME=My WissKI
DRUPAL_TRUSTED_HOSTS='^localhost$|^127\.0\.0\.1$'
DRUPAL_PRIVATE_FILES_DIR=/opt/drupal/private-files
DRUPAL_PROXY_ADDRESSES=none
WISSKI_STARTER_VERSION=true
WISSKI_DEFAULT_DATA_MODEL_VERSION=true
WISSKI_DEFAULT_GRAPH=https://my.wiss-ki.eu/

DB_DRIVER=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_NAME=wisski
DB_USER=wisski
DB_PASSWORD=wisski

PUBLIC_PORT=8080
DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_SUPERUSER_NAME=admin
DJANGO_SUPERUSER_PASSWORD=changeme
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,authproxy,nginx
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
RDF4J_TIMEOUT=5
TS_REPOSITORY=default
TS_USERNAME=admin
TS_PASSWORD=changeme
TS_TOKEN=

SOLR_PORT=8983
PGADMIN_PORT=8081
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
